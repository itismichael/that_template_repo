# Copy this file to .env and fill in your actual values.
# .env should be in .gitignore

# Project name - used for Docker image tagging by docker-compose.yml
# Should match 'project.name' in pyproject.toml and PROJECT_NAME in Taskfile.yml
PROJECT_NAME=your_core_library

# Application settings
APP_HOST=0.0.0.0
APP_PORT=8000
LOG_LEVEL=info

# Example: Database URL (if you use one)
# DATABASE_URL="postgresql://user:password@host:port/database"

# Example: External API Keys
# SOME_API_KEY="your_api_key_here"

# Development specific settings
# PYTHON_DEV_MODE=true # Example, if your app uses this
